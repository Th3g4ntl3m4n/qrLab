sudo bash -lc 'cat >/etc/apache2/sites-available/qrlab.conf <<EOF
<VirtualHost *:80>
    ServerName http://ec2-54-80-33-224.compute-1.amazonaws.com/
    DocumentRoot /var/www/qrLab/public

    <Directory /var/www/qrLab/public>
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog  \${APACHE_LOG_DIR}/qrlab_error.log
    CustomLog \${APACHE_LOG_DIR}/qrlab_access.log combined

    <FilesMatch "\.php$">
        SetHandler "proxy:unix:/run/php/php8.2-fpm.sock|fcgi://localhost/"
    </FilesMatch>
</VirtualHost>
EOF'